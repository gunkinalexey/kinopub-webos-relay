## v0.9.75 — Скрыт выбор сезона/серии там, где выбирать нечего

Backend всегда группирует медиа хотя бы в один псевдо-сезон
(`entry.get('season') or 1`), поэтому даже одиночный фильм с одним файлом
получал `item.seasons = [{'number': 1, 'episodes': [фильм]}]` — непустой
список. Фронтенд проверял только `!seasons.length`, а это условие после такой
группировки никогда не срабатывает, поэтому пилюля «Сезоны: 1» и лента из
одной серии рисовались даже для фильма.

Правило теперь такое:

- **2+ реальных сезона** — пилюли сезонов, как раньше.
- **1 сезон (в том числе синтезированный) с несколькими сериями** —
  плоская лента серий без пилюль: это плоский мини-сериал без разбивки по
  сезонам у KinoPub, показывать выбор из одного пункта незачем.
- **Один файл** (большинство фильмов) — ни пилюль, ни ленты; кнопки
  «Смотреть»/«Продолжить» уже достаточно.

Код `S01E01` на карточке серии и в подписи «Продолжить» теперь тоже показывается
только при реальных нескольких сезонах — для плоского списка серий он был
формальностью без объекта сравнения (сезон один и тот же для всех серий).

## v0.9.74 — Действия и серии наверх, продолжение просмотра

Порядок на экране описания: заголовок → кнопки просмотра → сезоны и серии →
постер с таблицей характеристик. Раньше кнопка «Смотреть» пряталась в колонке
постера, а выбор серии лежал под всей таблицей.

**Две кнопки вместо одной.** Если у записи есть сохранённая позиция, вместо
«Смотреть» появляются «▶ Продолжить 12:34» и «Начать заново», а рядом —
подпись с серией, к которой относится позиция (`S01E02 · Ставки`). Для сериала
продолжается именно та серия, которую бросили, а не первая.

Позицией считается только середина: не первые 30 секунд, не последняя минута и
не досмотренное до конца. Иначе кнопка «продолжить» предлагала бы вернуться
на 00:07 или к финальным титрам.

**Попутный дефект.** `play()` не сбрасывал `playerResumePosition`, поэтому
позиция перетекала с предыдущего фильма: открыв второй, вы стартовали с тайминга
первого. Стартовая позиция стала явным аргументом `play(item, episode, startAt)`.

`GET /history` принимает `media_id`, чтобы экран описания спрашивал позицию
одной записи, а не тянул весь список.

## v0.9.73 — Настоящий широкий фон вместо растянутого постера

У KinoPub на CDN есть отдельный 16:9 фон, который клиент не использовал:

| Путь | Размер | Соотношение |
|---|---|---|
| `poster/item/small/{id}.jpg` | 165×250 | 2:3 |
| `poster/item/medium/{id}.jpg` | 250×375 | 2:3 |
| `poster/item/big/{id}.jpg` | 500×750 | 2:3 |
| **`poster/item/wide/{id}.jpg`** | **до 3840×2160** | **16:9** |

Фон брался из `medium` — портретной картинки 250×375, растянутой на полосу
шириной 1380: отсюда и мягкость, и обрезка. Теперь `backdrop` строится из
`wide`, а постер — из `big`.

- Прокси получил параметр `fallback`: CSS-фон не умеет повторять запрос при 404,
  поэтому подстановка происходит на сервере. У 20 проверенных фильмов `wide` был
  всегда; запас нужен для редких исключений.
- Фон запрашивается как 1920×1080 вместо 1280×720.
- Высота полосы масштабируется — `clamp(280px, 32vh, 430px)` вместо жёстких
  300px, кадр смещён на `center 28%`.
- Content-Type у CDN всегда `image/jpeg`, но байты иногда WebP; Pillow
  определяет формат по содержимому, так что это уже работало.

Оговорка про постер: при текущих размерах блоков (250px под постер) выигрыш от
`big` почти не виден — он проявится на экране с DPR больше единицы, где под
250 CSS-пикселей нужно 500 физических.

## v0.9.72 — Авторизация терялась при F5

`/auth/status` был объявлен как `-> Dict[str, bool]`, а в 0.9.70 в него
добавились `has_refresh_token` и `expires_in`. FastAPI валидирует ответ по этой
аннотации, и `expires_in: 86399` булевым не является — ветка авторизованного
пользователя отдавала **500**.

Ветка неавторизованного возвращала только `bool` и работала, поэтому ломалось
ровно после успешной привязки и ровно при перезагрузке: фронтенд получал 500,
уходил в `.catch` и показывал экран привязки.

Аннотация исправлена на `Dict[str, Any]`. Остальные ручки с узкими аннотациями
проверены — расхождений нет.

Добавлен [backend/smoke_test.py](backend/smoke_test.py): поднимает приложение с
временной базой и посаженной сессией и дёргает ручки, которым не нужен KinoPub.
Ловит именно этот класс ошибок, который не видят ни `ast.parse`, ни `pyflakes`.
Проверено в обе стороны: с ошибкой — 4 FAIL и код возврата 1, без неё — 17 PASS.

```bash
docker run --rm -v "$PWD/backend:/src:ro" -w /app kinopub-webos-client-backend sh -c "cp /src/smoke_test.py . && python smoke_test.py"
```

## Автоматический коммит и пересборка

`.claude/settings.json` вешает хук `Stop`, который после каждого ответа Claude
запускает [.claude/hooks/checkpoint.sh](.claude/hooks/checkpoint.sh): коммитит
изменения и пересобирает backend, если он менялся.

Почему именно так:

- **`Stop`, а не `PostToolUse`.** Один ответ обычно содержит десяток правок;
  на `PostToolUse` это дало бы десяток коммитов и минуты пересборок на одно
  логическое изменение.
- **Только `backend/`.** Фронтенд смонтирован как read-only bind mount, его
  правки видны сразу — пересобирать нечего.
- **Не `rebuid_containers.bat`.** Он заканчивается `pause` (в хуке это висело бы
  до таймаута) и использует `build --no-cache`, то есть переустанавливает ffmpeg
  на каждый запуск. Скрипт остаётся для ручной полной пересборки; хук вызывает
  `docker compose up -d --build backend` с кэшем.
- `examples/` добавлен в `.gitignore`: 14 МБ чужих ассетов, и в каждой странице
  живой `csrf-token` той сессии, в которой её сохранили.

Отключить или отредактировать — через `/hooks`.

## v0.9.71 — Авторизация сбрасывалась после привязки устройства

Ответ `POST /auth/device/poll` объявлял `Content-Length: 0`, неся при этом
23 байта тела. Причина — `JSONResponse(..., headers=dict(response.headers))`:
копия заголовков внедрённого пустого `Response` приносила его `content-length: 0`,
а Starlette не перезаписывает длину, заданную вызывающей стороной.

Клиент читал ноль байт, `r.json()` падал, и ветка «authorized» не выполнялась —
хотя `Set-Cookie` при этом доходил и сессия на сервере создавалась. Экран
привязки оставался на месте, пользователь привязывал устройство заново, и каждая
попытка добавляла ещё одну сессию в базу. Токены при этом были совершенно
исправны: срок 24 часа, refresh-токен на месте.

- Cookie ставится на тот ответ, который реально возвращается, без копирования
  чужих заголовков.
- Если опрос всё-таки не удался, клиент сперва переспрашивает `/auth/status` и
  считает привязку успешной, если сессия уже активна.
- В Диагностике появились строки «Сессия», «Refresh-токен» и «Токен истекает
  через».

Проверка на неопределённые имена (`pyflakes`, `eslint no-undef`) добавлена в
обиход после регрессии 0.9.70 — она же поймала бы её сразу.

## v0.9.70 — Починка каталога и фильтра истории

- **Каталог отдавал 500 на всех разделах.** В `catalog_list` остался
  `'type': api_type` в возвращаемом словаре после перехода на `selector` —
  `NameError`. `ast.parse` такое не ловит, поэтому в проверку добавлен
  `pyflakes` для backend и `eslint --rule no-undef` для frontend.
- **`v1/history` игнорирует параметр `type`** — возвращает одну и ту же
  страницу при любом значении, что видно по `raw=48` для всех типов при
  `count` 3 / 45 / 0. Фильтрация в пределах одной страницы давала неполные
  выдачи (3 фильма вместо 48). Теперь при выборе типа backend обходит до 20
  страниц по 50 записей, собирает все подходящие и нарезает их на страницы сам;
  результат кэшируется на 3 минуты. Без фильтра — по-прежнему прямой
  постраничный проход.
- `/auth/status` теперь сообщает `has_refresh_token` и `expires_in`, а при
  привязке устройства без refresh-токена в лог пишется явное предупреждение:
  такая сессия закончится вместе с access-токеном.

## v0.9.69 — История, 3D и починенное Аниме

**Аниме — это жанр, а не тип.** Раздел был пустым, потому что клиент запрашивал
`GET /v1/items?type=anime`, а такого типа в KinoPub не существует. В списке
жанров на сайте «Аниме» стоит под id 25, между «Документальный» (24) и
«Короткометражка» (26), а страница `/anime` — это тот же фильтр по жанру.
Теперь раздел запрашивает `genre=25` и охватывает и фильмы, и сериалы.

Секции описываются одной таблицей `CATALOG_SECTIONS`, где раздел задаётся либо
типом, либо жанром, вместо прежнего словаря «раздел → тип».

**3D** оказался самостоятельным типом `3d` (`kino.watch/3d`), а не фильтром
внутри Фильмов. Появился маршрут `3d`, а ссылка «3D» в заголовке каталога
раньше вела обратно на список фильмов — теперь это переключатель
**Фильмы / 3D**, активный вариант подсвечен.

**История просмотров** — новый рабочий раздел по образцу `kino.watch/history`:

- `GET /catalog/history` поверх `v1/history`, с постраничной выдачей;
- записи сгруппированы по дням, как на сайте, с заголовками «Сегодня»,
  «Вчера» и датой прописью дальше;
- фильтр по типу: Все, Фильмы, Сериалы, 3D, Концерты, Докуфильмы, Докусериалы,
  ТВ Шоу — тип передаётся наверх и дополнительно проверяется на бэкенде,
  так как в документации API он не описан;
- страницы считаются отдельно для каждого фильтра;
- карточки те же, что в каталоге, поэтому переход в описание работает как везде.

Локальный `/history` (позиции воспроизведения этого моста) не тронут — новый
эндпойнт называется `/catalog/history`, чтобы их не путать.

## v0.9.68 — Полноэкранный режим плеера

Пикселей это не добавляет: слой плеера и так `position:fixed; inset:0`, видео
растянуто на весь вьюпорт, а браузер webOS сам по себе полноэкранный. Смысл в
другом — ТВ-браузеры обычно отдают полноэкранное видео **аппаратной
видеоплоскости** вместо композитинга страницы, а именно она делает
HDR-passthrough и аппаратное масштабирование 4K. Это тот же механизм, что и
Direct вместо MSE.

Повышение до видеоплоскости, как правило, требует полного экрана на самом
элементе `<video>`, а не на контейнере, — поэтому режим выбирается в настройках:

- **Оболочка плеера** (по умолчанию) — свои кнопки, таймлайн и меню остаются;
- **Само видео** — лучший шанс на HDR, но управление становится системным;
- **Выключен**.

Запрос делается синхронно в момент клика по «Смотреть»: к моменту ответа API
окно пользовательской активации уже закрыто и браузер откажет. Отказ
перехватывается и не ломает запуск. Закрытие плеера выходит из полного экрана;
выход по кнопке пульта не путается с закрытием плеера.

В Диагностику добавлены строки **Декодируется** (реальные `videoWidth×videoHeight`
— видно, приехал ли настоящий 4K) и **Полный экран** (режим и активность).

## v0.9.67 — Максимальное качество картинки под возможности устройства

Раньше по умолчанию всегда играл 1080p H.264, а 2160p HEVC оказывался
последним в списке кандидатов — даже 720p H.264 обходил 1080p HEVC. Поскольку
10-битный цвет и HDR у KinoPub приезжают именно в HEVC, вместе с разрешением
терялся и диапазон цветов.

**Ранжирование потоков (backend)**

- `choose_best_stream` больше не «целится в 1080p». Порядок: разрешение по
  убыванию → HEVC перед H.264 при равном размере → простой HTTP-файл, чтобы
  клиент мог отдать его аппаратному декодеру.
- Прежняя формула `abs(height - 1080)` штрафовала 2160p ровно так же, как 0p,
  поэтому 4K не выбиралось никогда.
- Поток получает флаги `hevc`, `hdr` и `bitrate` (HDR определяется по полю
  KinoPub, а если его нет — по строке качества).

**Выбор под устройство (frontend)**

- Возможности проверяются один раз: `canPlayType` и `MediaSource.isTypeSupported`
  для HEVC Main10 и H.264, плюс `(dynamic-range: high)` и `(color-gamut: p3)`.
- Берётся самый высокий вариант, который устройство реально декодирует. Нет
  HEVC — тихо откатывается к H.264, а не падает.
- Для HEVC, который декодируется аппаратно, используется прямое
  воспроизведение: MSE (hls.js) на webOS обычно роняет HDR в SDR, а нативный
  путь отдаёт файл декодеру телевизора.
- Если прямой поток не открылся, один раз происходит откат на relay/HLS с
  сохранением позиции и понятным сообщением.
- Настройка «Качество» наконец работает — теперь это потолок:
  авто / 2160p / 1080p / 720p. Явно выбранный режим потока имеет приоритет.

**Диагностика** показывает, что устройство реально умеет: ответ `canPlayType`
по H.264 и HEVC, наличие MSE, HDR-дисплей, гамут P3, текущий потолок и то,
какой вариант и транспорт играют сейчас.

Оговорка: приложение работает во встроенном браузере webOS, и переведёт ли он
панель в HDR-режим — зависит от платформы, а не от клиента. 10-битный HEVC даёт
выигрыш по бандингу в любом случае; включился ли настоящий HDR, покажет
Диагностика и индикатор HDR на самом телевизоре.

## v0.9.66 — Описание стало экраном, а не оверлеем

Панель карточки больше не накрывает каталог, а заменяет его — как поиск и
настройки. Сайдбар и строка поиска остаются на месте, прокручивается только
область контента.

- Введён единый переключатель экранов `showScreen`. Раньше `route` и `doSearch`
  вручную дёргали `classList` у каждого экрана по отдельности; экранов стало
  четыре: каталог, поиск, настройки, описание.
- Возврат идёт туда, откуда пришли: из результата поиска — обратно в поиск, из
  каталога — в каталог. Фокус возвращается на ту же карточку, если она ещё на
  странице.
- Кнопка «← Назад» вместо крестика модалки, в левом верхнем углу постера.
- Кнопка Back на пульте: сначала закрываются открытые модалки (Explorer,
  Диагностика, авторизация, подписка), затем экран описания, и только потом
  происходит переход на «Новинки».
- Переход в плеер не изменился: кнопка «Смотреть» или выбор серии.

## v0.9.65 — Панель описания по образцу страницы KinoPub

Панель карточки переработана по четырём сохранённым страницам сайта из
`examples/`. Раскладка теперь повторяет оригинал: постер и действия слева,
табы и таблица характеристик справа, полоса сезонов и лента серий снизу.
Переход в плеер — кнопкой «Смотреть» или выбором серии.

**Frontend**

- Двухколоночная сетка: слева постер, счётчик 👍/👎 и «Смотреть»; справа табы
  «Сюжет / Аудио / Субтитры» и блок «Информация».
- Таблица характеристик: Рейтинг (КП и IMDb с числом голосов), Всего, Статус,
  Год выхода, Страна, Жанр, Режиссёр, В ролях, Длительность, Качество, Субтитры.
  Строка появляется только если данные есть.
- Полоса сезонов с переключением и лента серий: превью, бейдж «Эпизод N»,
  название и код вида `S02E01`.
- Подпись аудиодорожки теперь «Русский · Дубляж · LostFilm», как на сайте:
  раньше студия шла перед типом перевода.

**Backend**

- `/catalog/items/{id}` отдаёт поля, нужные панели: `countries`, `director`,
  `cast`, `duration`, `quality`, `votes`, `imdb_votes`, `kinopoisk_votes`,
  `subtitle_langs`, `audio_langs`, `seasons_count`, `episodes_count`, `finished`.
- Счётчики 👍/👎 берутся из явных полей, а если их нет — восстанавливаются из
  net-баланса и общего числа голосов.
- Сезоны сортируются по номеру.

Удалён осиротевший CSS: `.episode-row`, `.episode`, `.hero-meta`.

## v0.9.64 — Ложная ошибка «play() interrupted by pause()»

`video.play()` возвращает промис, который отклоняется с `AbortError`, если до
фактического старта воспроизведения элемент поставили на паузу или перезагрузили.
Это происходит при **каждом** переключении качества, дорожки и потока и при
закрытии плеера — то есть штатно. Код передавал текст этой ошибки в `mediaError`,
и поверх плеера появлялось «The play() request was interrupted by a call to
pause(). Позиция 00:00 сохранена».

- Прерванный запуск (`AbortError` или сообщение с `interrupted`) больше не
  считается ошибкой воспроизведения.
- Попытка запуска привязана к номеру переключения потока: отказ, относящийся к
  уже смененному потоку, игнорируется.
- `NotAllowedError` (блокировка автозапуска браузером) теперь показывает
  осмысленное «Нажмите OK, чтобы начать воспроизведение», а не общую ошибку.
- Настоящие ошибки (например, неподдерживаемый кодек) по-прежнему показываются.
- Приписка «Позиция … сохранена» выводится, только когда позиция действительно
  больше нуля.

## v0.9.63 — Аудит и чистка кода

Бизнес-логика не менялась; удалено только то, что недостижимо, и исправлена одна
реальная ошибка управления.

**Исправлено**

- Кнопка OK на пульте при фокусе на таймлайне перематывала видео в 00:00.
  Таймлайн обрабатывает только стрелки, поэтому Enter уходил в
  `activeElement.click()`, а синтетическое событие приходит с `clientX = 0` —
  это считалось кликом по левому краю полосы. Теперь синтетический клик
  (`detail === 0`) ставит паузу/воспроизведение, а не перематывает.

**Удалено (недостижимый код)**

- `GET /catalog/home`, `GET /mock/home`, `GET /mock/items/{id}` и `KPApi.home` —
  ни один вызов из клиента на них не приходил.
- Функции `bestStream`, `optionText`.
- Поля состояния, в которые только писали и никогда не читали: `hlsUsing`,
  `lastSearch`, `selectedSuggestion`, `profileTimer`, `home`, `progressMap`.
- Неиспользуемый CSS-класс `.brand-svg`.

**Упрощено**

- Разбор пагинации существовал в двух копиях (`catalog_list` и
  `_pagination_values`), включая two определения `first_int`. Осталась одна.
- Две одинаковые замыкания сборки подписи дорожек заменены общими
  `pushLabelPart` / `truthyFlag` / `labelWithIndex`. Подписи проверены тестами:
  вывод не изменился.

**Найдено, но НЕ тронуто** — это изменило бы поведение, решать вам:

- Настройки «Качество», «Язык аудио», «Автопереход к следующей серии» и
  «Уменьшить анимации» никуда не подключены: значения сохраняются, но не
  читаются (`reduce_motion` вешает класс, которого нет в CSS).
- `KPApi.search` при любой ошибке подставляет мок-каталог, поэтому истёкшая
  сессия показывает «Дюну» и «Оппенгеймера» как результаты поиска.
- Тестовый поток в Диагностике пишет прогресс с `media_id = "diagnostic"`.
- `/catalog/compare-feeds` со снимком айдишников от 2026-08-05 — разовый
  инструмент сравнения сортировок, живёт в API Explorer.
- `POST /auth/logout` оставлен, хотя кнопки выхода в UI нет.

## v0.9.62 — Блок субтитров в плеере

- **Исправлен бесконечный цикл.** Добавление `<track>` вызывает событие `addtrack`,
  обработчик которого снова звал `applySubtitleChoice`, тот удалял дорожку и
  создавал заново — и так по кругу. Один выбор субтитров порождал бесконечную
  череду элементов, каждый заново скачивал VTT с backend, и субтитры не
  удерживались на экране. Теперь пересборка происходит, только если реально
  изменился выбор, ссылка или сдвиг.
- Собственная вставленная дорожка больше не считается дорожкой потока: раньше она
  добавляла лишний пункт в меню при каждом переключении.
- Добавлено оформление `::cue` — раньше в CSS не было ни одного правила, и
  субтитры выводились дефолтным размером браузера, мелким для телевизора.
- Работает настройка `subtitle_size`, которая до этого была захардкожена в 100 и
  ни на что не влияла: четыре размера, переключатель прямо в плеере и в настройках.
- Настройка «Субтитры» (ru/en) применяется при запуске: подходящая дорожка
  выбирается автоматически.
- Учитывается `subtitles[].shift` из KinoPub и складывается со смещением
  локального HLS. Backend теперь умеет сдвигать таймкоды в обе стороны.
- Недоступные дорожки (встроенные PGS без ссылки) помечены «· недоступны», а не
  просто заблокированы без объяснения.
- Ширина выпадающих списков ограничена, чтобы длинная подпись дорожки не
  переносила ряд управления на вторую строку.

## v0.9.61 — Переключение звука без FFmpeg, когда это возможно

Смена дорожки идёт по лестнице: FFmpeg включается только если в самом потоке
действительно одна дорожка.

1. **hls.js alternate audio** — если в манифесте есть `#EXT-X-MEDIA:TYPE=AUDIO`,
   дорожка меняется на месте. Позиция и перемотка не затрагиваются вообще.
2. **Нативные `video.audioTracks`** — работает на LG webOS при Direct-воспроизведении
   MP4. Desktop Chrome их не отдаёт и проваливается на следующий шаг.
3. **Другой HLS-вариант KinoPub** — backend (`GET /media/audio-variants`) проверяет
   `hls4`, `hls2`, `hls` и сообщает, сколько в каждом альтернативных аудиодорожек.
   Первый подходящий вариант перезагружается с сохранением позиции. Результат
   проверки кэшируется, поэтому следующие переключения бесплатны.
4. **Серверный remux** — прежний путь через FFmpeg, теперь только как fallback.

Исправления:

- `import re` отсутствовал, хотя `re.split` вызывался при слиянии дублирующихся
  media-узлов: карточка сериала могла падать с 500.
- `/catalog/items/{id}/play` запрашивает `media-links` не только когда нет потоков,
  но и когда `audios` короче, чем заявляет `tracks`, и **дополняет** список вместо
  замены. Это основная причина «в меню одна дорожка».
- `result.audios || result.media.audios` во фронтенде никогда не срабатывал:
  пустой массив истинный, поэтому список из media-узла терялся.
- Дорожка адресуется позицией в списке (`-map 0:a:N`), а не `audios[].index`.
  Прежний код выводил из `index` абсолютный номер потока: при 0-based payload
  вторая дорожка отображалась на первую, а обложка в MP4 сдвигала индексы.
- Убраны `-bsf:v h264_mp4toannexb` (HLS-муксер ставит фильтр сам), `discardcorrupt`
  (молча выбрасывал пакеты при переподключении к CDN) и `first_pts=0`
  (рассинхрон при старте не с нуля).
- Набор HTTP-опций FFmpeg определяется по установленному бинарю: неизвестная
  опция фатальна и убила бы задачу до старта.
- Упавшая задача больше не удерживает ключ дедупликации — повтор запускает новый
  процесс, а не отдаёт тот же труп до истечения TTL.

Внешние субтитры не зависят от выбора дорожки ни на одном из шагов.

## v0.9.60 — Full KinoPub audio metadata list

- The audio selector now uses the complete `audios` array returned by KinoPub.
- `media.tracks` no longer hides entries from the selector because production responses can contain only the active/default track there.
- Duplicate media nodes from `/v1/items/<id>` are merged instead of keeping the first occurrence. Streams, audio metadata, subtitles, and track numbers are combined.
- Playback logs now include `audio_count` and raw `tracks` for diagnostics.
- Cached HLS preparation from v0.9.59 remains unchanged.

## v0.9.59 — Resilient cached audio HLS

Исправлен обрыв подготовки дорожки на больших HTTP-файлах KinoPub: FFmpeg теперь повторно подключается к CDN после преждевременного TLS/HTTP EOF, повторяет временные сетевые ошибки и отбрасывает повреждённый неполный пакет. Видео по-прежнему копируется без перекодирования, аудио преобразуется в AAC, а готовый HLS используется для обычной перемотки.

- При выборе озвучки backend создаёт локальный HLS из исходного HTTP-файла KinoPub.
- Видео копируется без перекодирования, выбранный звук преобразуется в AAC 2.0.
- Текущий поток останавливается только на время подготовки первых сегментов.
- После подключения перемотка работает внутри уже созданного HLS; новый FFmpeg-процесс нужен только при переходе за пределы подготовленного диапазона.
- Подготовка выполняется отдельной задачей с опросом статуса и тайм-аутом 45 секунд, поэтому интерфейс не зависает на сообщении о подключении.
- Субтитры остаются внешними WebVTT; при HLS, начатом не с нулевой секунды, backend автоматически сдвигает таймкоды.
- Неактивные FFmpeg-задачи останавливаются, временные сегменты удаляются автоматически.

# KP TV — webOS-oriented KinoPub bridge

Лёгкий TV-веб-клиент и FastAPI-прослойка для браузеров LG webOS.

## Реализовано

- Device Flow с динамическим `verification_uri`, таймером и повторным получением кода;
- сохранение пользовательской сессии в SQLite;
- автоматическое обновление access token;
- mock API для разработки без реального каталога KinoPub;
- главная страница, поиск, карточка фильма/сериала и список серий;
- настройки качества, режима потока, языка, субтитров и анимаций;
- локальная серверная история просмотра и восстановление позиции;
- прямое воспроизведение, byte-range relay, HLS relay и локальный HLS для выбранной озвучки;
- переписывание URL в HLS playlist, включая аудио, ключи и субтитры;
- защита relay от localhost/private IP и проверка redirect;
- управление пультом, восстановление фокуса между экранами;
- диагностика кодеков и тест произвольного URL в трёх режимах;
- backend/frontend media-логи.

## Запуск

```bash
cp .env.example .env
# заполнить KINOPUB_CLIENT_ID и KINOPUB_CLIENT_SECRET
docker compose up --build
```

Откройте `http://IP_СЕРВЕРА:8080` в браузере телевизора.

## Что работает без KinoPub API

Главная, поиск, карточки, сериалы, настройки и история работают через `/mock/*`. В демонстрационных карточках нет видеоссылок. Для проверки плеера откройте **Диагностика** и вставьте доступный вам URL.

## Следующий этап

Снять реальные JSON-ответы каталога, карточки, сезонов и файлов KinoPub и реализовать один backend-adapter, преобразующий их во внутренние модели клиента.

## Production

Задайте `STREAM_HOST_SUFFIXES`, включите HTTPS и `COOKIE_SECURE=true`. Для публичного многопользовательского сервиса добавьте шифрование токенов at-rest, CSRF-защиту и rate limiting.


## API Explorer (v0.6.0)

После авторизации откройте кнопку **API Explorer** в верхнем меню. Explorer выполняет только GET-запросы через текущую backend-сессию.

- путь вводится без домена и без начального `/`, например `v1/items`;
- query-параметры вводятся отдельно: `page=0&perpage=20`;
- OAuth endpoint заблокирован;
- access/refresh token, cookie, secret и похожие поля маскируются;
- максимальный размер диагностического ответа — 2 MB;
- ответ можно скопировать или скачать как JSON.

Точные пути KinoPub API зависят от актуальной версии сервиса. Explorer нужен, чтобы безопасно снять структуру реальных ответов и затем написать нормализующий adapter. Не публикуйте скачанные ответы без просмотра: в данных аккаунта могут быть персональные сведения, даже если токены уже замаскированы.

## Первый реальный запуск видео (v0.9.0)

После авторизации главная страница загружает реальные списки KinoPub. При открытии карточки backend запрашивает `/v1/items/{id}`, строит список фильмов/серий и при нажатии «Смотреть» получает поток через `/v1/items/media-links?mid=...`. Приоритет: H.264, обычный HLS, около 1080p. Поток запускается через HLS relay.

Если воспроизведение не началось, откройте «Диагностика» и посмотрите последние события `Play option resolved`, `HLS playlist relayed` или ошибку media.


## 0.9.17
- Pagination remains available when KinoPub shortcut endpoints omit total page counts.
- Unknown totals show pages in blocks of ten; `>>` advances to the next block.


## v0.9.24
Standalone catalogue sections now use `/v1/items` (`feed=all`) instead of `/v1/items/fresh`. The New releases tabs continue to use popular/fresh/hot shortcuts.


## Новинки: фиксированные источники

- Популярные: `GET /v1/items/popular?type=movie`
- Свежие: `GET /v1/items/fresh?type=movie`



## v0.9.40
- Круглые иконки управления плеером вместо текстовых кнопок.
- Выбор Direct / Relay / HLS relay перенесён в выпадающий список справа.


## 0.9.48
- Более подробные подписи звуковых дорожек: язык, перевод/студия, тип озвучки, каналы, кодек и битрейт, когда эти данные доступны.


## 0.9.48
- Исправлен вывод `[object Object]` в названиях аудиодорожек.
- Добавлен безопасный разбор вложенных полей студии, перевода и типа озвучки.
- Нестроковые метаданные без понятного названия больше не выводятся.


## v0.9.48
- Подробные подписи субтитров: язык, название, переводчик/студия, тип, SDH/форсированные и формат.
- Вложенные объекты метаданных больше не отображаются как `[object Object]`.


## v0.9.48
- Сохраняет подробные названия аудиодорожек KinoPub после `loadedmetadata` на LG webOS.
- Нативные `audioTracks` используются для фактического переключения, а подписи объединяются с API-метаданными по индексу.
- Общие названия `Track 1` / `Дорожка 1` больше не затирают студию, тип перевода, каналы и кодек.


## v0.9.55
- Исправлена перемотка при серверной звуковой дорожке: целевая позиция передаётся напрямую в FFmpeg-поток.
- Перемотка больше не показывает сообщение о подключении дорожки.
- Таймлайн сразу отображает выбранную позицию во время короткого переподключения.


## 0.9.55 — HLS audio switching

Выбор звуковой дорожки переводит текущий вариант качества на соответствующий HLS-поток и переключает дорожку через hls.js. Поток меняется только при выборе аудио; последующая перемотка выполняется обычным seek без повторного FFmpeg-remux.


## 0.9.55
- Restored missing logicalCurrentTime/logicalDuration helpers.
- Restored timeline click seek handler.
- Fixes ReferenceError cascade that broke seeking, progress saving and audio switching.


## v0.9.55
- Убрано ошибочное переключение по HLS-аудиогруппам: KinoPub HLS часто содержит только одну дорожку.
- Альтернативная аудиодорожка подключается через серверный remux.
- Аудио перекодируется в AAC 2.0 для совместимости Chrome и LG webOS; видео копируется без перекодирования.
- Позиция сохраняется при смене дорожки и при перемотке.


## v0.9.56 — Reliable audio source selection

- Quality variants are grouped instead of discarding duplicate HTTP/HLS links.
- Normal playback uses the URL appropriate for Direct/Relay/HLS mode.
- Server-side audio switching always uses the original HTTP MP4 source, because KinoPub HLS variants often contain only the default audio track.
- FFprobe validates the requested absolute audio stream index before FFmpeg starts.
- AC3/EAC3 and other tracks are converted to browser-compatible AAC 2.0.
- The backend waits for the first playable MP4 fragment and returns a clear error instead of silently opening a stream without audio.


## v0.9.57 — Simplified native tracks

- Removed the unstable FFmpeg full-video remux endpoint and the FFmpeg Docker dependency.
- Audio switching now uses only mechanisms provided by the actual stream:
  - alternate HLS audio groups through hls.js, when KinoPub's manifest contains them;
  - native `HTMLMediaElement.audioTracks` on LG webOS Direct HTTP playback.
- Selecting audio automatically changes the current stream to Direct HTTP when needed, while preserving playback time.
- The KinoPub `tracks` field is used to filter audio choices that belong to the selected media.
- Desktop Chrome does not expose MP4 audio tracks, so the selector reports the limitation and returns to Auto instead of hanging.
- External KinoPub subtitles remain supported through the SRT/ASS-to-WebVTT relay and do not require FFmpeg.


## Как работает переключение звука в v0.9.60

KinoPub HLS обычно содержит только одну дорожку, а метаданные остальных озвучек относятся к исходному HTTP-файлу. Поэтому клиент не пытается переключать несуществующие HLS audio groups. При выборе дорожки backend:

1. проверяет абсолютный индекс через FFprobe;
2. запускает FFmpeg с `-c:v copy` и AAC 2.0 для звука;
3. создаёт EVENT HLS по 4 секунды на сегмент;
4. возвращает плееру URL после появления достаточного диапазона;
5. продолжает подготовку в фоне, расширяя доступный диапазон перемотки.

При закрытии плеера, возврате на «Авто», смене качества или режима активная задача останавливается. Временные данные находятся в `/data/audio_hls`.

